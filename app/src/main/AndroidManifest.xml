<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.angelova.w510.radixapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menuItems.OfferActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".menuItems.OrderActivity" />
        <activity android:name=".menuItems.PromotionsActivity" />
        <activity android:name=".menuItems.PricesActivity" />
        <activity android:name=".menuItems.LanguagesActivity" />
        <activity android:name=".menuItems.AboutActivity" />
        <activity android:name=".RegisterActivity"></activity>
    </application>

</manifest>